# load data into memory from disk - cleandf only
filepath = ...
df = read_unstr_df(filepath, 'unstr')

# filter data with time bin>2 only


# create columns for transitions 

# create plot for all choice transitions

# create plot for rewarded, unrewarded and diff transitions

# create plot for rewarded, unrewarded and diff transitions (average of all animals)

# create plot for switch-only trantisitions 

# create plot for each comparison calling above functions